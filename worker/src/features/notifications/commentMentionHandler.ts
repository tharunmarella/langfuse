/**
 * Comment mention notifications - stubbed for PostgreSQL-only mode.
 * Requires full repository functionality.
 */

export const handleCommentMentions = async () => {
  // Stubbed in PostgreSQL-only mode
  return;
};

export const handleCommentMentionNotification = async (_job?: any) => {
  // Stubbed in PostgreSQL-only mode
  return;
};
